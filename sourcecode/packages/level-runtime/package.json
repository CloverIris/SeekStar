{
  "name": "@seekstar/level-runtime",
  "version": "0.0.0",
  "private": true,
  "description": "SeekStar chunked level runtime for AI Cartographer terrain bands.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "seekstar-level-runtime": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\""
  },
  "dependencies": {
    "@seekstar/ai-service": "0.0.0",
    "@seekstar/core-schema": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0"
  }
}
