{
  "name": "@seekstar/storage-service",
  "version": "0.0.0",
  "private": true,
  "description": "SeekStar atomic JSON world repository.",
  "type": "module",
  "main": "dist/index.js",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\""
  },
  "dependencies": {
    "@seekstar/core-schema": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0"
  }
}
