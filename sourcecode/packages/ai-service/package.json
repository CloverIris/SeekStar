{
  "name": "@seekstar/ai-service",
  "version": "0.0.0",
  "private": true,
  "description": "SeekStar AI service contracts, context packets, and key-handoff boundary.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "seekstar-ai": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\""
  },
  "dependencies": {
    "@seekstar/core-schema": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0"
  }
}
