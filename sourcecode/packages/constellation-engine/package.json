{
  "name": "@seekstar/constellation-engine",
  "version": "0.0.0",
  "private": true,
  "description": "SeekStar constellation core engine and Pixi runtime projection boundary.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "seekstar-engine": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "node -e \"fs.rmSync('dist', { recursive: true, force: true })\""
  },
  "dependencies": {
    "@seekstar/core-schema": "0.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.0.0"
  }
}
